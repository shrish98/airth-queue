{
  "name": "airth-job-queue-root",
  "version": "1.0.0",
  "private": true,
  "description": "Mini Job Queue Dashboard root runner for Airth assignment",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"magenta.bold,cyan.bold\" \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "test:e2e": "cd backend && npm run test:e2e"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
